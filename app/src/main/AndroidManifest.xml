<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projekchat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjekChat">
        <activity android:name=".ui.home.ui.invitation.InvitationActivity"></activity>
        <activity
            android:name=".ui.coba.COBA"
            android:label="@string/title_activity_c_o_b"
            android:theme="@style/Theme.ProjekChat.NoActionBar" />
        <activity
            android:name=".ui.Coba"
            android:label="@string/title_activity_coba"
            android:theme="@style/Theme.ProjekChat.NoActionBar" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.home.ui.searchfriend.SearchFriendActivity" />
        <activity
            android:name=".ui.regis.RegisterActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>